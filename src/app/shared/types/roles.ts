export enum mainRole {
  ADMIN,
  PROJECTMANAGER,
  EMPLOYEE,
  TEAMLEAD,
  MANAGER,
}

export enum roleAction {
  CREATE,
  VIEW,
  UPDATE,
  APPROVE,
  DELETE,
}

export enum persona {
  ACTIVE,
  INACTIVE,
}

export enum entityEdit {
  ALL,
  ENTITY,
  MULTIPLE_ENTITIES,
  NONE,
}

export enum module {
  INVOICES,
  PROJECTS,
  WORKINGHOURSREGISTRATION,
}
